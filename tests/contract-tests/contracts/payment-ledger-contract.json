{
  "consumer": {
    "name": "openpaynet-payment-service"
  },
  "provider": {
    "name": "openpaynet-ledger-service"
  },
  "interactions": [
    {
      "description": "a processed payment can be fetched from the ledger by transaction id",
      "request": {
        "method": "GET",
        "pathTemplate": "/v1/ledger/{txn_id}"
      },
      "response": {
        "status": 200,
        "requiredFields": {
          "txn_id": "string",
          "user_id": "string",
          "amount": "number",
          "currency": "string",
          "payment_type": "string",
          "status": "string",
          "processor_status": "string",
          "final_status": "string",
          "decision_reason": "string",
          "processing_stage": "string",
          "trace_id": "string",
          "idempotency_key": "string",
          "timestamp": "string"
        },
        "allowedPaymentTypes": ["credit", "p2p", "b2b"],
        "allowedFinalStatuses": ["approved", "declined"]
      }
    }
  ],
  "metadata": {
    "contractType": "pact-style-http",
    "generatedBy": "codex",
    "version": "1.0.0"
  }
}
